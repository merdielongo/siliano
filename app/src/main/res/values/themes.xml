<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Campasiliano" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/segoeui</item>
    </style>

    <style name="MenuStyle" parent="Theme.MaterialComponents.DayNight">
        <item name="background">@color/white</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="MenuTextStyle" parent="TextAppearance.AppCompat">
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ThemeOverlay.TopAppBar" parent="ThemeOverlay.MaterialComponents.BottomAppBar.Primary">
        <item name="colorOnPrimary">@color/purple_200</item>
        <item name="iconTint">@color/purple_700</item>
        <item name="titleEnabled">false</item>
    </style>

    <style name="MyBottomAppBar" parent="Widget.MaterialComponents.BottomAppBar">
        <item name="materialThemeOverlay">@style/ThemeOverlay.BottomAppBar</item>
    </style>

    <style name="ThemeOverlay.BottomAppBar" parent="ThemeOverlay.MaterialComponents.BottomAppBar.Primary">
        <item name="colorOnPrimary">@color/white</item>
        <item name="iconTint">@color/white</item>
        <item name="titleEnabled">false</item>
    </style>

    <style name="bottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="my_dialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="windowMinWidthMajor">70%</item>
        <item name="windowMinWidthMinor">70%</item>
    </style>

</resources>